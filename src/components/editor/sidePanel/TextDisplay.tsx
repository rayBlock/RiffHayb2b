









export function TextDisplay() {


    // const mapMidData = (data: MainDataObject['mid']) => {
    //     return data.map((item) => {
    //         const { fs, ...rest } = item;
    //         const entry = { fs } as Record<string, string | number>;

    //         // Handle other properties and add them to the 'entry' object
    //         for (const key in rest) {
    //             entry[key] = rest[key];
    //         }

    //         return entry;
    //     });
    // };

    // // Usage
    // const midData = mapMidData(textMidData);
    // console.log(midData, "midddy");

    return (
        <div className='grid gap-4 '>




        </div>
    )
}